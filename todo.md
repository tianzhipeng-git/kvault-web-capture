现在这个tool(尤其是py)的capacity乱搞
现在那个ks Adapter也是玩闹呢, Defuddle之类的也得重新做
页面上配合的新config的也没改呢

<br />

还有一个可配置项，就是页面标准化URL要去除哪些参数，现在已经写死了一些，但是未来可能会为每一个站点配置一些去重时不需要的参数
一个仅对现有所有已有base的页面进行"打标+规则"的功能, 不爬网站
清理下载目录

截图：每次请求启动独立浏览器

<br />

multica: 项目的首页再加一个功能，叫链接展开。如果贴的是一个SideMap，就是SideMap递归展开，如果贴的是一个单页的URL，就临时给它跑一下，然后把里面的子链接全给出来

<br />

<br />

#### 简易提交入口

系统设置里可以将一个项目下的一个站点配为整个系统的一个默认站点。

* 在选择项目那个页面增加一个快捷入口，贴url提任务, 其本质就是在这个默认站点下，重跑已选的那个弹窗里面手填URL那个功能。

* 在这里提交完之后，就跳转到这个默认站点的正式运行页面，查看当前提交的运行的状态。

* 在正式运行那个页面加一个导出按钮，对应的就是把运行对应的所有page ID导出，所以功能也是复用现有的页面清单那个导出弹窗

* 我其实会手动给这个站点配置爬取深度是0，然后配置一些默认的规则

这一套下来相当于实现了一个简易爬取入口, 不用搞复杂化

<br />

上面这个流程 在webui的接口层面应该有这3个接口: 提交得到允许ID, 查询运行ID状态, 根据运行ID下载文件

但是我希望再额外加两个牛逼的同步阻塞等待接口:

* 提交后直接得到结果(下载标准导出格式的zip)

* markdown接口提交后直接得到markdown格式结果返回

