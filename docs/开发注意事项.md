## 前端开发注意事项

- 弹窗、卡片、表格、flex/grid 列表里展示 URL、文件路径、hash、长 ID 等不可断长字符串时，必须同时处理容器和文本：外层容器加视口宽度兜底（如 `w-[calc(100vw-2rem)]`、`max-w-*`、`overflow-hidden`），flex/grid 子项加 `min-w-0` / `flex-1`，文本按场景使用 `break-all`、`break-words` 或受控 `truncate`。不要只依赖 `max-w-*` 或单独 `truncate`，否则子元素默认 `min-width:auto` 仍可能把弹窗撑爆。